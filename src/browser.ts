import { checkGitHub } from '#github-up/lib/github';

export default { checkGitHub };
export { checkGitHub };

export type { ComponentStatus, IncidentImpactValue, IncidentStatusValue, Result } from '#github-up/types';
export type { Component, Incident, Summary } from '#github-up/types';
