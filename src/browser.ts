import { checkGitHub } from '#github-up/lib/github';

export default { checkGitHub };
export { checkGitHub };

export type { ComponentStatus, IncidentImpactValue, IncidentStatusValue, Result } from '#github-up/lib/types';

export type { Component, Incident, Summary } from 'statuspage.io';
