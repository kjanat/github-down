import { checkGitHub } from '#github-down/lib/github';

export default { checkGitHub };
export { checkGitHub };

export type { ComponentStatus, IncidentImpactValue, IncidentStatusValue, Result } from '#github-down/lib/types';

export type { Component, Incident, Summary } from 'statuspage.io';
